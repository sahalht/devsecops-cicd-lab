name: GitGuardian Secret Scan (JSON)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  secret-scan:
    name: GitGuardian Secret Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # scan full git history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install GitGuardian CLI
        run: pip install ggshield

      # ❌ NO ggshield auth login here

      - name: Run GitGuardian secret scan (JSON)
        env:
          GITGUARDIAN_API_KEY: ${{ secrets.GITGUARDIAN_API_KEY }}
        run: |
          ggshield secret scan repo . --json > gitguardian-report.json || true

      - name: Upload GitGuardian JSON report
        uses: actions/upload-artifact@v4
        with:
          name: gitguardian-secret-report
          path: gitguardian-report.json
